import styled from "styled-components";
import bg from "../../assets/backgrounds/blob.png";

const PageBG = styled.div`
  min-height: 100vh;
  background: url(${bg}) no-repeat;
  background-size: cover;
  background-position: center;
`;

export default PageBG;
